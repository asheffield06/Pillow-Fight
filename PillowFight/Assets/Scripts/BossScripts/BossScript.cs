using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BossScript : MonoBehaviour
{
    //Aims for this boss, when it gets hit it spews out chickens
    //It flees from the players like the chickens do

    //The damage the player deals to either the boss or to the other player, directly translates to 
    // how many chickens come out of them.

    public int BossHealth = 100;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
